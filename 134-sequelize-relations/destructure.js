// создание объекта (структуризация)
const obj = { a: 1 };

// деструктуризация - достаём данные из объекта и кладём в константы
const { a } = obj;

// 1. слева от равно
const { x, y } = myObj;
// 2. в параметрах функции
function myFun({ x, y }) {}
